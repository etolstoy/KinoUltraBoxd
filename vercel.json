{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "KinoUltraBoxd/**"
        ]
      }
    }
  ],
  "routes": [
    { "src": "/api/webhook", "dest": "api/webhook.ts" }
  ]
} 