{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/webhook", "dest": "api/webhook.ts" }
  ]
} 